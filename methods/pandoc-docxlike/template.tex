\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{fontspec}
\usepackage{setspace}

% Word-like fonts (requires XeLaTeX)
\setmainfont{Times New Roman}
% Fallback to Arial if Calibri is unavailable
\setsansfont{Arial}

\hypersetup{colorlinks=true, urlcolor=blue, linkcolor=black}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Word-like line spacing
\setstretch{1.15}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[R]{\small A.L. Woods, \thepage}

% Uppercase section heading macro (no rule)
\newcommand{\sectionheading}[1]{%
  \vspace{1.2ex}%
  {\large\bfseries\MakeUppercase{#1}}\par\vspace{0.4ex}%
}

% Word-like spacing for lists
\setlist[itemize]{noitemsep, topsep=2pt, leftmargin=1.5em}

\begin{document}

% Header using metadata (inline name + contact, then affiliations + address)
\noindent\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}l r}
  {$name$} & \href{mailto:$email$}{$email$} \;|\; \href{$website$}{$website$} \\
\end{tabular*}

$for(affiliations)$
\noindent $affiliations$\\
$endfor$
$if(address)$
\noindent $address$
$endif$

$if(summary)$

$summary$

$endif$

$body$

\end{document}
