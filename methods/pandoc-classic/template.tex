\documentclass[12pt]{article}
\usepackage[top=1in,bottom=1in,left=0.75in,right=0.75in]{geometry}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{fontspec}
\usepackage{setspace}

% "Classic" tuned to match the Word reference metrics
% Prefer Calibri for body; fallback to Helvetica/Arial/Heros (Word-like)
\IfFontExistsTF{Calibri}{\setmainfont{Calibri}[Scale=MatchLowercase]}{%
  \IfFontExistsTF{Helvetica}{\setmainfont{Helvetica}[Scale=MatchLowercase]}{%
    \IfFontExistsTF{Arial}{\setmainfont{Arial}[Scale=MatchLowercase]}{\setmainfont{TeX Gyre Heros}[Scale=MatchLowercase]}%
  }%
}
\IfFontExistsTF{Helvetica}{\setsansfont{Helvetica}}{%
  \IfFontExistsTF{Arial}{\setsansfont{Arial}}{\setsansfont{TeX Gyre Heros}}%
}

\hypersetup{colorlinks=true, urlcolor=black, linkcolor=black}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Match Word-like leading
\setstretch{1.15}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[R]{\small A.L. Woods, \thepage}

% Section heading with thin rule (shared with Lua filter)
\newcommand{\sectionheading}[1]{%
  \vspace{1.2ex}%
  {\large\bfseries\MakeUppercase{#1}}\par\vspace{0.3ex}%
  {\color[gray]{0.6}\rule{0.98\linewidth}{0.3pt}}\vspace{0.6ex}%
}

% Conservative list spacing
\setlist[itemize]{noitemsep, topsep=0.4ex, itemsep=0pt, leftmargin=1.25em, labelsep=0.5em}

\begin{document}

% Header using metadata
\noindent\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}l r}
  {\textbf{$name$}} & $email$ \;|\; $website$ \\
\end{tabular*}
\vspace{1.2ex}

$for(affiliations)$
\noindent $affiliations$\\
$endfor$
$if(address)$
\noindent $address$
$endif$

$body$

\end{document}
