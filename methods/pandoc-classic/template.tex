\documentclass[12pt]{article}
\usepackage[top=1in,bottom=1in,left=0.75in,right=0.75in]{geometry}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{fontspec}
\usepackage{setspace}

% "Classic" look: serif body, conservative spacing
% Prefer TeX Gyre Termes (Times-like), widely available with TeX Live
% Prefer TeX Gyre Termes; otherwise try common system fonts, then Heros
\IfFontExistsTF{TeX Gyre Termes}{\setmainfont{TeX Gyre Termes}[Scale=MatchLowercase]}{%
  \IfFontExistsTF{Times New Roman}{\setmainfont{Times New Roman}[Scale=MatchLowercase]}{%
    \IfFontExistsTF{Times}{\setmainfont{Times}[Scale=MatchLowercase]}{\setmainfont{TeX Gyre Heros}[Scale=MatchLowercase]}%
  }%
}
\IfFontExistsTF{Helvetica}{\setsansfont{Helvetica}}{%
  \IfFontExistsTF{Arial}{\setsansfont{Arial}}{\setsansfont{TeX Gyre Heros}}%
}

\hypersetup{colorlinks=true, urlcolor=black, linkcolor=black}
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}

% Slightly tighter than default, but readable
\setstretch{1.10}

\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[R]{\small A.L. Woods, \thepage}

% Section heading with thin rule (shared with Lua filter)
\newcommand{\sectionheading}[1]{%
  \vspace{1ex}%
  {\large\bfseries\MakeUppercase{#1}}\par\vspace{0.25ex}%
  {\color[gray]{0.6}\rule{0.98\linewidth}{0.3pt}}\vspace{0.5ex}%
}

% Conservative list spacing
\setlist[itemize]{noitemsep, topsep=0.4ex, itemsep=0pt, leftmargin=1.25em, labelsep=0.5em}

\begin{document}

% Header using metadata
\noindent\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}}l r}
  {\textbf{$name$}} & $email$ \;|\; $website$ \\
\end{tabular*}
\vspace{1ex}

$for(affiliations)$
\noindent $affiliations$\\
$endfor$
$if(address)$
\noindent $address$
$endif$

$body$

\end{document}
